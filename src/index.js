import cases from './cases';

// parent.window.admcloseExpand = utils.admcloseExpand;


// function Execute(data) {
//   if (data.type === 1) {
//     const case1160 = new cases.Case1160({ admid: data.admid, urlClick: data.urlClick, urlLogExpand: data.urlLogExpand, admExpandPram: data.admExpandPram, callback: data.callback });
//   } else if (data.type === 2) {
//     const sideKick = new cases.SideKick(data);

//     sideKick.renderBanner();
//   }
// }

// function case1160(data) {
//   new cases.Case1160({ admid: data.admid, urlClick: data.urlClick, urlLogExpand: data.urlLogExpand, admExpandPram: data.admExpandPram, callback: data.callback });
// }

export { cases };
export default { cases };
